  
  {% if ( obj.main_title or obj.main_description )%}
  <div  class="{{ obj.custom_container ? obj.custom_container : 'container'}} {{ obj.main_text_align }} px-0 pt-4 pb-1">
    <h1 class="pb-0 my-0">{{ obj.main_title|raw }}</h1>
    <p class="px-4">{{ obj.main_description|raw }}</p>
  </div>
  {% endif %}